export enum MangaStatus {
  CONTINUE = '0',
  COMPLETE = '1',
}

export enum MangaSortType {
  BY_CREATED = '0',
  BY_TOP_ALL = '1',
  BY_TOP_YEAR = '2',
  BY_TOP_MONTH = '3',
  BY_TOP_WEEK = '4',
}

export enum MangaSortDate {
  WEEK = 'week',
  MONTH = 'month',
  YEAR = 'year',
}

export const DATE_FORMAT = {
  DATE: 'MM-DD-YYYY',
}
